import { Routes } from '@angular/router';
import { WeatherForcastComponent } from './weather-forcast/weather-forcast.component';

export const routes: Routes = [{
    path:'', component: WeatherForcastComponent
}];
